services:
  dev:
    build:
      context: .
      dockerfile: Containerfile
    working_dir: /workspace
    volumes:
      # Host-visible file exchange area only. Do normal project work in the
      # container-local /workspace directory, not in this mounted path.
      - ./shared:/shared
    tty: true
    stdin_open: true
    command: sleep infinity
