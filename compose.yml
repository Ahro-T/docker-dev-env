services:
  dev:
    build:
      context: .
      dockerfile: Containerfile
    working_dir: /workspace
    volumes:
      - ./workspace:/workspace
    tty: true
    stdin_open: true
    command: sleep infinity
